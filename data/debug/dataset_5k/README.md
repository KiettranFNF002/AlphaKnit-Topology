Debug-only dataset location.

- `dataset_5k` is for smoke tests / diagnostics only.
- Do **not** use this path for long training runs.
- Use `data/processed/dataset` (or WebDataset shards) for real training.
